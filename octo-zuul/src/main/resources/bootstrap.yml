spring:
  application:
    name: octo-zuul
server:
  port: 8765

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
#    serviceUrl:
#      defaultZone: http://localhost:8761/octo-eureka/



hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false