spring:
  application:
    name: octo-zuul
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:9000}
server:
  port: 8765
  
hystrix:
 command:
   default:
     execution:
       timeout:
         enabled: false