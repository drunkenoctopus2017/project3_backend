spring:
  application:
    name: octo-zuul
#  cloud:
#    config:
#      uri: ${SPRING_CONFIG_URI:http://18.217.119.188:9000}
server:
  port: 8765
#hystrix:
# command:
#   default:
#     execution:
#       timeout:
#         enabled: false

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true 
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false

zuul:
  routes: 
    octo-auth:
      path: /octo-auth/**
      sensitiveHeaders: 
      serviceId: octo-auth
