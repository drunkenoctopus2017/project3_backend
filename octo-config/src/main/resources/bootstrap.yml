#config
server:
  port: 9000
spring:
  application:
    name: octo-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/drunkenoctopus2017/project3_config.git